---
import { caseStudies } from '@/data/homePageContent';
---

<section class="py-20 bg-gradient-to-br from-sage-50 to-white" data-animate>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-serif font-bold text-navy-900 mb-4">
        Історії трансформації
      </h2>
      <p class="text-lg text-navy-700 max-w-3xl mx-auto">
        Реальні результати моїх клієнтів, які змінили своє життя та бізнес
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      {caseStudies.map((study) => (
        <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow" data-animate>
          <div class="p-8">
            {/* Header */}
            <div class="mb-6">
              <h3 class="text-2xl font-serif font-bold text-navy-900 mb-2">
                {study.clientName}
              </h3>
              <p class="text-gold-600 font-medium mb-4">
                {study.profession}
              </p>
              <div class="flex flex-wrap gap-4 text-sm text-navy-600">
                <div class="flex items-center gap-2">
                  <svg class="w-5 h-5 text-sage-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  <span>{study.timeInMentorship}</span>
                </div>
                <div class="flex items-center gap-2">
                  <svg class="w-5 h-5 text-sage-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                  </svg>
                  <span>{study.sessionsCount} сесій</span>
                </div>
                <div class="flex items-center gap-2">
                  <svg class="w-5 h-5 text-gold-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/>
                  </svg>
                  <span class="font-semibold text-gold-600">+{study.incomeGrowth} дохід</span>
                </div>
              </div>
            </div>

            {/* Before */}
            <div class="mb-6">
              <h4 class="text-lg font-semibold text-navy-900 mb-3 flex items-center gap-2">
                <span class="w-2 h-2 bg-red-500 rounded-full"></span>
                Було
              </h4>
              <div class="bg-red-50 rounded-lg p-4">
                <p class="text-navy-700 font-medium mb-2">
                  {study.before.employment}
                </p>
                <ul class="space-y-2">
                  {study.before.challenges.map((challenge) => (
                    <li class="flex items-start gap-2 text-navy-600">
                      <svg class="w-5 h-5 text-red-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                      </svg>
                      <span>{challenge}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>

            {/* After */}
            <div>
              <h4 class="text-lg font-semibold text-navy-900 mb-3 flex items-center gap-2">
                <span class="w-2 h-2 bg-green-500 rounded-full"></span>
                Стало
              </h4>
              <div class="bg-green-50 rounded-lg p-4">
                <ul class="space-y-2">
                  {study.after.achievements.map((achievement) => (
                    <li class="flex items-start gap-2 text-navy-700">
                      <svg class="w-5 h-5 text-green-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                      </svg>
                      <span>{achievement}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    {/* CTA */}
    <div class="text-center mt-12">
      <p class="text-lg text-navy-700 mb-6">
        Хочете досягти подібних результатів?
      </p>
      <button
        id="case-studies-cta"
        type="button"
        class="bg-gold-500 text-navy-900 px-8 py-4 text-lg font-medium rounded-lg hover:bg-gold-400 transition-colors focus:ring-2 focus:ring-offset-2 focus:ring-gold-500 shadow-lg hover:shadow-xl"
      >
        Записатись на консультацію
      </button>
    </div>
  </div>
</section>

<script>
  import { useUIStore } from '@/stores/uiStore';

  const caseStudiesCTA = document.getElementById('case-studies-cta');
  if (caseStudiesCTA) {
    caseStudiesCTA.addEventListener('click', () => {
      useUIStore.getState().openConsultationModal();
    });
  }
</script>

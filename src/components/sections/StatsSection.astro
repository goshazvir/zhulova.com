---
import { statistics } from '@/data/homePageContent';
---

<section id="about" class="py-20 bg-white" data-animate>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-serif font-bold text-navy-900 mb-4">
        Досвід, якому можна довіряти
      </h2>
      <p class="text-lg text-navy-700 max-w-3xl mx-auto">
        Допомагаю людям досягати своїх цілей за допомогою професійного коучингу, медитацій та НЛП
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-0">
      {statistics.map((stat, index) => {
        const isFirstInRow = index === 0 || (index % 2 === 0);
        return (
          <div
            class={`text-center py-8 px-6 lg:px-8 transition-all hover:bg-gray-50/50
              ${index > 0 ? 'border-t md:border-t-0' : ''}
              ${index > 0 && !isFirstInRow ? 'md:border-l' : ''}
              ${index > 0 ? 'lg:border-t-0 lg:border-l' : ''}`}
            data-animate
            style={`animation-delay: ${index * 100}ms`}
          >
            <div class="text-5xl md:text-6xl font-serif font-bold text-gold-800 mb-4 leading-none">
              {stat.value}
            </div>
            <div class="text-navy-700 font-medium text-sm md:text-base max-w-[200px] mx-auto leading-relaxed">
              {stat.label}
            </div>
          </div>
        );
      })}
    </div>
  </div>
</section>
